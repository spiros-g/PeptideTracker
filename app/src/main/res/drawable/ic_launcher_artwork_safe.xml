<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        Keep the full square artwork inside the adaptive-icon safe area.
        OEM launchers apply their own circle/squircle mask around this layer.
    -->
    <item
        android:left="16dp"
        android:top="16dp"
        android:right="16dp"
        android:bottom="16dp"
        android:drawable="@drawable/app_icon_generated" />
</layer-list>
